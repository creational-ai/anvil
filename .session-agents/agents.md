# Agents

<!--
Roster of session agents installed in this project. The install script appends
`- <role>` bullets below as roles are added; keep the list flat.

Optional: add a `## Routing` section at the bottom to declare project-internal
routing rules (e.g., "hub-and-spoke via PM", "specialists escalate cross-cutting
findings to architect first"). If no `## Routing` section exists, the skill's
default applies (free specialist-to-specialist; coordinator when in doubt).

See `~/.claude/skills/session-agents/references/setup.md` for tailoring guidance
and `references/comms-format.md` for the inbox format.
-->

- architect
- qa
