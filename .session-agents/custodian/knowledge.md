# Custodian — Knowledge

Durable per-project knowledge for the custodian role. Re-read on every activation.

*See `~/.claude/skills/session-agents/references/knowledge.md` for what belongs here vs. doesn't.*

---

## Authoritative refs (re-read on activation)

- `.session-agents/agents.md`, `CLAUDE.md`, `~/.claude/skills/session-agents/references/{setup,migration,activation}.md`

*Add any project-specific authoring conventions or external references the custodian should re-read on activation.*

---

## Role roster + overlay map

*Which roles this project runs, which are standard (baseline + overlay) vs. custom (overlay IS the definition), and where each overlay deviates from its baseline. The source of truth is `agents.md`; record the load-bearing deltas here.*

---

## Promotion history

*Project roles promoted (or being prepared) into shipped skill baselines: which role, what split (baseline vs. overlay), audit verdict, handoff state to skillwright. Future-you needs to know what's already generic.*

---

## Layer-scope decisions + conventions

*This project's authoring conventions — draft-folder location, naming, routing rules — and any load-bearing layer-scope calls (why a given line lives in `_common` vs. baseline vs. overlay). The why, so the next authoring pass doesn't re-litigate.*

---

## Anti-patterns to flag

*Project-specific authoring/leanness hazards beyond the standard custodian anti-patterns in the skill.*
